# Week 16 — Final Exam

**Covers:** Weeks 9–15 (PyTorch basics; CNNs; GNNs; NLP & word embeddings; RNNs; LLMs & agentic AI; reinforcement learning). Cumulative understanding from weeks 1–8 may also be assumed as background.

No new project this week — class time is reserved for the final exam.

## Suggested exam prep

- Re-run each of weeks 9–15's "Try it yourself" exercises without looking at the solution first
- Be able to explain, in one sentence each: tensors & the training loop,
  convolution & pooling, a GCN's message-passing update, word embeddings,
  what an RNN/LSTM carries forward between steps, the tool-use agent loop, and
  the explore/exploit tradeoff in reinforcement learning
- Be able to say which architecture from weeks 9–15 you'd reach for first,
  given a new problem described in plain language (an image task → CNN; a
  social network → GNN; free text → embeddings/LLM; a sequence of past values
  → RNN; a game/robot that learns from trial and error → RL)
